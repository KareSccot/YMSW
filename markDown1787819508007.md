AI总结

对话围绕代码部署流程优化展开，重点讨论token动态配置方案、CI/CD集成实现、包版本控制策略及回滚机制。涉及Helm Chart调用逻辑、OPS协作规范及PSPS认证争议等技术细节，强调版本化管理和历史追溯的重要性。

以上由AI大模型生成，可能包含不准确的信息。

说话人1 00:00:00

我看那个包应该不需要更多解释了。你按人来写，主要是给你两个token。因为不能把token硬编码到代码里，所以让他写的时候，可能需要通过配置。

我预想是通过CI那边的variables配置，作为环境变量，启动时按相应方式启动即可。具体方式在Readme里其实就是个示例包。

如果你让AI帮你形成新的Toml包，它应该会把这些都带出来。说到这个，头可能不是在Readme里体现，而是存进去。OK，明白这个过程，到时候再问。

你说需要他说不清楚吗？我现在没有特别明确的，反正那个Jar包也打出来了。打出来之后需要去项目里拉，把包也改好。

然后可能需要他把那个token放到不能硬编码的代码里，再按照我们之前的Skill写一下。我没仔细看，现在上传之后再部署一下应该就可以了。

对，先让它调一下，要求我提了。调完之后我们再看一下，合进主分支之前，拉下来就可以合到后面走CD的标准流程。

如果你后面有Code Review，或者打包过程中已经有数据过程，那是我自己写的一个脚本。后面我每次肯定会自己打那个Artifact，从那儿拉就行了。

那Helm它不用给你后面的O，它不会去拉你的Chart，不会去拉打进黑包里的那个包。它会告诉OK，去我特定的仓库里面，拉它的Helm Chart工具。本来应该不会去拉你的那个配置。

它启动的时候，让启动的去拉，就是本质上它是一个OPS的，他是OPS的人。那我知道你是启动的时候去直拉包，那个包是放在我看到你SharePoint的那个截图。

这个截图是在一个里面有专门有个Package对这个。他拉这个镜像以后，我每次都会把这个发布成最新的放在那。这个，你考虑版本化一下，就是我建议你有个版本化。

你现在看就你可以永远换一个最新的这样的。比如说同时打包一个版本，对这种典型的都是这个发版。你出了个bug就回退，就是别人可能回不去。

只有你某一个人操作都是不可以回本，你要回本肯定要有历史版本才能有回退。你自己想一个，因为你这是一个特殊的发布逻辑。我前每天对，但是原则上是你要有发版就要能有回归。

然后都能够可以回滚。所以说，你拉倒可以，你拉只是一个从最近拉打一个Tag，但是版本每一次是可以放。不是有多个一个东西就是把同时生两个包。

一个是他的版本包，最新包的话，像就不用改了他每一对对我觉得那个是OK的，但是你需要可以看到你。然后还有一个Tag打的是他那个版本包。

然后对因为因为你其实看那个包的话，它是有一个Hash的嘛，所以，你看上就能看出来，它是当地记录是报在哪。但是，你是本身就回溯的时候，你就是按一发版去做的。

所以，你一想像是会有一个发版。发版其实是比如说有流水线，有发布流程。

如果看多这个答案，他们也可以跟K8s集成，我们很小没有意义。

其实昨天那个还没有解决，就是问题点在于PSPS文件到底用不用来认证。

我们能不能约人直接聊一下这个事情？这对我们最后的底子是有关系的。

现在当前我们做存储，有没有GPN？是不是要对它的使用存储多一些了解？